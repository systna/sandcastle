# Task

Task {{TASK_ID}} has passed review. Close it — and do nothing else.

Run the close command: `{{CLOSE_TASK_COMMAND}}` with `<ID>` replaced by
{{TASK_ID}}.

# Rules

- Close ONLY task {{TASK_ID}}.
- Do NOT make any code changes.
- Do NOT pick up another task.

Once the task is closed, output <promise>COMPLETE</promise>.
